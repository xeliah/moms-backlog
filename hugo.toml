baseURL = "https://momsbacklog.com/"
locale = "en-us"
title = "Mom's Backlog"
paginate = 10
disableKinds = ["taxonomy", "term"]

[params]
  tagline = "A product manager's backlog for raising a very strong-willed kid."
  positioning = "Burnt-out parent, closet product manager, or both? Same job, different outfit. This is the backlog I keep for the hardest, least documented product I've ever worked on — proof that parenting and PM-ing run on the exact same skillset."
  author = "Mom's Backlog"

[markup.goldmark.renderer]
  unsafe = true

[markup.highlight]
  noClasses = false
